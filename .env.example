# Leipzig Haushaltsdaten - Environment Configuration

# Static data - no database configuration needed!
# Budget data is served as static JSON files from /public/data/
# Just run: npm install && npm run dev

# Optional: Analytics (only for production)
# NEXT_PUBLIC_MATOMO_URL="https://your-matomo-instance.com"
# NEXT_PUBLIC_MATOMO_SITE_ID="1"
